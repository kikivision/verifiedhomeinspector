// The one value the site and the brand builder both need. The builder cannot
// be imported by a page (it writes files on import), so the tile lives here.
//
// Two thirds of the way from the site navy (#1D2B3A) toward the old favicon
// blue (#22456E). Chosen 2026-09-12 from five candidates at 16/32/64px: the
// closest to the site that still reads blue in a browser tab.
export const TILE = '#203C5C';
